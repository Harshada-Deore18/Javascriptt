//console.dir function
//console.log shows the element DOM tree
//console.dir shows the element as an object with its properties

//inner html
/*the inner HTML allows to get the html inside the element as a string.
    It is valid only for element nodes .For other node types we can use nodeValue or data-->
    <!--the outer HTML contains the full HTML . innerHTML + element itself-->

    <!-- Text content -
    provides the access to the text inside the element: only text, minus all tags-->
    <!-- Hidden property-
    hidden attribute and the DOM property specifies whether the element is visible or not*/